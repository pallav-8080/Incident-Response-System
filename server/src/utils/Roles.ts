/**
 * Keep  the file in sync with client/src/utils/roles.ts
 */
export enum ROLES {
  CITIZEN = 'Citizen',
  DISPATCH = 'Dispatch',
  POLICE = 'Police',
  FIRE = 'Fire',
  NURSE = 'Nurse',
  ADMINISTRATOR = 'Administrator',
}

export default ROLES
